import React from "react";

const RecruitList = () => {
	// APIたたいてrecruitsテーブルのレコードを全て取得してRecruitCardを使って表示する
	// recruits.map((recruit) => <RecruitCard {...recruit} />)みたいな。
	return <div>RecruitList</div>;
};

export default RecruitList;
